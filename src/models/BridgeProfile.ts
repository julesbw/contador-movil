export type BridgeProfile = {
  id: string
  name: string
  baseUrl: string
  token: string
  sourceId?: string
  sourceName?: string
  createdAt: string
  updatedAt: string
  lastVerifiedAt?: string
}
